node_modules
dist
.turbo
.DS_Store
.env
coverage
legacy/node_modules
legacy/venv

# TypeScript incremental build state
*.tsbuildinfo

# Tauri build output
src-tauri/target
src-tauri/gen

# Shipment documents are never committed — they are customer data.
src/test/fixtures/*.pdf
