{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The app's own commands. No plugin permissions are granted — everything the shell does is one of the four commands in lib.rs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
